# Fichier docker-compose.yml complet avec services, volumes et networks (4a)
services:
  # BDD 
  db:
    image: postgres:16-alpine
    container_name: generic-db
    environment:
    # Gestion et Utilisation d’un fichier de configuration .env et variables correctement externes (1a et 4c)
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - db-data:/var/lib/postgresql/data
      - ./db-init:/docker-entrypoint-initdb.d
    # Définition et utilisation des healthchecks(4b)
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  # API (Backend)
  api:
    build: 
      context: ./api 
    ports:
      - "${HOST_API_PORT}:${API_PORT}" 
    environment: 
      DB_HOST: ${DB_HOST}
      DB_USER: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      API_PORT: ${API_PORT}
    depends_on: 
      db:
        condition: service_healthy 
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:${API_PORT}/status"]
      interval: 10s
      timeout: 3s
      retries: 5
    
  # Front
  frontend:
    build:
      context: ./frontend
    ports:
      - "${HOST_FRONT_PORT}:${FRONT_PORT}"
    environment:
      API_BASE_URL: ${API_BASE_URL}
    depends_on:
      api:
        condition: service_healthy

volumes:
  db-data: {} 